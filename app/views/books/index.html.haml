%h1
  %center Listing books
- @books.each_slice(3) do |books_array|
  .row
    - books_array.each do |book|
      .col-md-4.col-sm-4
        .thumbnail
          = link_to image_tag(book.image.url(:thumb)), book_path(book)
        .caption
          = "author: #{book.author}"
          %p= "title: #{book.title}"
          %p= book.description
  %br
= paginate @books
