%h1
  %center My books
- @books.each_slice(3) do |books_array|
  .row
    - books_array.each do |book|
      .col-md-4.col-sm-4
        .thumbnail
          = image_tag book.image.url(:thumb)
        .caption
          = "author: #{book.author}"
          %p= "title: #{book.title}"
          %p= book.description
          = link_to 'Show', user_book_path(@user, book)
          = link_to 'Edit', edit_user_book_path(@user, book)
          = link_to 'Destroy', user_book_path(@user, book), :method => :delete, :data => { :confirm => 'Are you sure?' }
  %br

%p= link_to 'New Book', new_user_book_path, class: 'btn btn-sm btn-success'
= paginate @books
